{
  "name": "tenant-coordinator",
  "version": "0.1.0",
  "description": "Tool for coordinating efforts, reimbursements, plans and rent payments with my tenant",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "npx coffee scripts/build.coffee",
    "clean": "rm -rf dist/ static/js/ tenant-coordinator.db* node_modules/.cache/ && echo 'Clean complete'",
    "clean:all": "npm run clean && rm -rf node_modules/ && echo 'Full clean complete - run npm install'",
    "dev": "npm run build && node dist/main.js",
    "start": "node dist/main.js",
    "test": "npm run build && node --test dist/tests/**/*_test.js",
    "lint": "echo 'Linting skipped - configure if needed'"
  },
  "dependencies": {
    "coffeescript": "^2.7.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "nodemailer": "^6.9.0",
    "stripe": "^17.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "chokidar": "^3.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Robert de Forest <robert@defore.st>",
  "license": "MIT"
}
